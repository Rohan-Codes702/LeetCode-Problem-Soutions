class Solution {
    public int countSegments(String s) {
        int count=0;
        String str= s.trim();
        if(str.isEmpty()){
            return 0;
        }
        String []arr=str.split("\\s+");
        int len=arr.length;
        return len;


        
        
    }
}